<div class ="container">
  <div class ="row-lg-8 md-9">

    <h3>注文情報入力</h3>
    <br>
    <br>
    <%= form_with model: @order, url: orders_confirm_path, method: :post do |f| %>
      <h4>支払方法</h4>
        <%= f.radio_button :order_method, Order.order_methods.key(0) %>
        <%= f.label :order_method_credit_card, Order.order_methods_i18n[:credit_card] %><br>

        <%= f.radio_button :order_method, Order.order_methods.key(1) %>
        <%= f.label :order_method_transfer, Order.order_methods_i18n[:transfer]%>

      <h4>お届け先</h4>
        <%= f.radio_button :select_address, 0 %><%= f.label :address_id, "ご自身の住所" %><br>
        <div>　　〒<%= current_customer.postcode %></div>
        <div>　　　<%= current_customer.address %></div>
        <div>　　　<%= current_customer.last_name %> <%= current_customer.first_name %></div>
        <br>
        <%= f.radio_button :select_address, 1 %><%= f.label :address_id, "登録済住所から選択" %><br>
        <%= f.select :address_id, options_from_collection_for_select(Delivery.all, :id, :address_display) %>
        <br>
        <%= f.radio_button :select_address, 2 %><%= f.label :address_id, "新しいお届け先" %><br>
        <table>
          <tr>
            <td>　　郵便番号 (ハイフンなし)</td>
            <td><%= f.text_field :postcode, placeholder: "0000000" %></td>
          </tr>
          <tr>
            <td>　　住所</td>
            <td><%= f.text_field :address, placeholder: "うどん県あげたま市" %></td>
          </tr>
          <tr>
            <td>　　宛名</td>
            <td><%= f.text_field :name, placeholder: "糖質制限" %></td>
          </tr>
        </table>
      <%= f.submit "確認画面へ進む", class: "btn btn-primary", method: :POST %>
    <% end %>

  </div>
</div>
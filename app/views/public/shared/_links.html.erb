<%- if controller_name != 'sessions' %>
  <h5>既に登録済みの方</h5>
  <%= link_to "こちら", new_customer_session_path(resource_name), class: "text-success" %>からログインしてください。<br />
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <h5>会員登録がお済みでない方</h5>
  <%= link_to "こちら", new_customer_registration_path(resource_name), class: "text-success" %>から新規会員登録を行ってください。<br />
<% end %>
<h3>ジャンル編集</h3>
<div>
  <%= form_with model: @genre, url: admin_genre_path, method: :patch do |f| %>
    <b><%= f.label :ジャンル %></b>
    <%= f.text_field :name %>
    <%= f.submit '変更を保存' %>
  <% end %>
</div>
<h3>ジャンル一覧・追加</h3>

<%= form_with model: @genre, url: admin_genres_path do |f| %>
  <h4>ジャンル</h4>
  <div>
    <%= f.text_field :name, placeholder: "ジャンル名" %>
  </div>
  <div>
    <%= f.submit '新規登録' %>
  </div>
<% end %>
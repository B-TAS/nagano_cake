<table class="table">
  <tr>
    <th class="table-dangre font-weight-bold"><i class="fas fa-heart color_red"></i> ジャンル検索</th>
  </tr>
  <% genre.each do |genre| %>
  <tr>
    <td class="font-size"><%= genre.name %></td>
  </tr>
  <% end %>
</table>